{
  "name": "@mall/store",
  "version": "0.2.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo store build",
    "test": "vitest run",
    "lint": "echo store lint",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zustand": "^5.0.5"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
