{
  "name": "@mall/store",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo store build",
    "test": "echo store test",
    "lint": "echo store lint",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zustand": "^5.0.5"
  }
}
